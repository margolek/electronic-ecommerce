var myCarousel = document.querySelector('#myCarousel')
var carousel = new bootstrap.Carousel(myCarousel, {
  interval: false,
})

// var myCarousel_batch = document.querySelector('#myCarousel-batch')
// var carousel_batch = new bootstrap.Carousel(myCarousel_batch, {
//   interval: false,
// })